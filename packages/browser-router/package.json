{
  "name": "@dwfe/browser-router",
  "version": "1.0.1",
  "license": "MIT",
  "main": "./dist/index.js",
  "typings": "./dist/types",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "npm run clean && tsc -b",
    "build watch": "npm run clean && tsc -b -w"
  },
  "devDependencies": {
    "@dwfe/path-resolver": "^1.0.1",
    "@types/node": "12",
    "history": "5",
    "rimraf": "3",
    "rxjs": "6",
    "typescript": "4"
  }
}
